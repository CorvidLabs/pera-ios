# PeraWallet_Classes_Demo — Context

## Overview

Architectural context for the `PeraWallet_Classes_Demo` module. The authoritative contract is `PeraWallet_Classes_Demo.spec.md`; the source files it covers are listed in that spec's `files:` frontmatter.

## Key files

See the spec `files:` list.

## Architectural decisions

- Record design decisions and trade-offs here as they are made.

## Current status

Spec status: `active`. Enforced by `specsync check --strict` in CI.
